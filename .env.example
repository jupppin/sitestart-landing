# Email Configuration (Gmail SMTP)
# To get a Gmail App Password:
# 1. Go to your Google Account > Security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to App passwords (search for it in Google Account)
# 4. Generate a new app password for "Mail"

GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
NOTIFICATION_EMAIL=your-email@gmail.com

# Database URL (SQLite - auto-generated, no changes needed)
DATABASE_URL="file:./dev.db"

# Cloudflare API (for deployment and DNS)
# To get these values:
# 1. Go to Cloudflare Dashboard > Overview
# 2. Account ID is in the right sidebar
# 3. For API Token: Go to My Profile > API Tokens > Create Token
#    Use "Edit Cloudflare Pages" template or create custom with Pages:Edit and DNS:Edit permissions
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_ZONE_ID is optional - zone ID is automatically looked up by domain name
# Only set this if you want to force a specific zone for all domains
# CLOUDFLARE_ZONE_ID=

# File Storage
UPLOAD_DIR=./uploads  # Local file storage directory
MAX_FILE_SIZE=10485760  # 10MB default
